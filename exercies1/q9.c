#include "graphics.h"

int main(void) {
  setColour(red);
  drawArc(200, 200, 100, 100, 0, 180);
  setColour(orange);
  drawArc(201, 201, 98, 98, 0, 180);
  setColour(yellow);
  drawArc(202, 202, 96, 96, 0, 180);
  setColour(green);
  drawArc(203, 203, 94, 94, 0, 180);
  setColour(blue);
  drawArc(204, 204, 92, 92, 0, 180);
  setColour(magenta);
  drawArc(205, 205, 90, 90, 0, 180);
  return 0;
}